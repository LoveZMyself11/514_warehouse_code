# Codex 提示词：微信聊天记录物品清单提取

## 任务描述

我需要从微信聊天记录中提取学部仓库的物品清单数据。聊天记录中约有300多条消息，包含图片和文字描述，以货架位置标记作为分隔。

## 数据格式说明

### 聊天记录结构
- 每个物品通常包含：图片 + 文字描述（物品名称，可能含数量）
- 多个物品描述后，会有一条位置标记消息（如 "a1", "b2", "地板", "门后"）
- 位置标记表示前面所有物品都在该位置

### 示例聊天记录
```
[图片]
kt板
[图片]  
彩色纸 5包
[图片]
剪刀 2把
a1
[图片]
胶带
[图片]
订书机
b2
```

**解读规则：**
- "kt板"、"彩色纸 5包"、"剪刀 2把" 都在 `a1` 位置
- "胶带"、"订书机" 都在 `b2` 位置

## 位置编码规范

请将所有位置标记规范化为：

| 原始输入 | 标准编码 | 说明 |
|---------|---------|------|
| a1, A1 | A1 | A货架第1层 |
| a2, A2 | A2 | A货架第2层 |
| a3, A3 | A3 | A货架第3层 |
| a4, A4 | A4 | A货架第4层 |
| b1-b4 | B1-B4 | B货架1-4层 |
| c1-c4 | C1-C4 | C货架1-4层 |
| d1-d4 | D1-D4 | D货架1-4层 |
| 地板, 地上 | FLOOR | 地板区域 |
| 门后 | DOOR | 门后区域 |

## 提取要求

请分析整个聊天记录，提取所有物品信息，生成结构化数据：

### 输出格式1：CSV
```csv
编号,物品名称,位置,数量,原始描述
ITEM0001,kt板,A2,若干,kt板
ITEM0002,彩色纸,A2,5包,彩色纸 5包
ITEM0003,剪刀,A2,2把,剪刀 2把
ITEM0004,胶带,B3,若干,胶带
```

### 输出格式2：JSON
```json
[
  {
    "编号": "ITEM0001",
    "物品名称": "kt板",
    "位置": "A2",
    "数量": "若干",
    "原始描述": "kt板"
  },
  {
    "编号": "ITEM0002",
    "物品名称": "彩色纸",
    "位置": "A2",
    "数量": "5包",
    "原始描述": "彩色纸 5包"
  }
]
```

## 数据处理规则

1. **位置分配**：每个物品的位置由其后面第一个出现的位置标记决定
2. **数量识别**：从描述中提取数字+量词（如 "5包"、"2把"），没有则填 "若干"
3. **物品名称**：去除数量后的剩余文字
4. **编号生成**：按顺序生成 ITEM0001, ITEM0002...
5. **忽略内容**：跳过 "[图片]"、"[Image]" 等标记
6. **去重处理**：如果同一位置有完全相同的物品名称，合并并累加数量

## 特殊情况处理

- 如果消息中同时包含物品描述和位置标记（如 "胶带 a1"），按 "胶带在a1" 处理
- 如果位置标记不规范（如 "A一"、"a-1"），尝试智能识别后规范化
- 如果某个物品描述前没有任何位置标记，标记为 "UNKNOWN"

## 验证要求

输出结果后，请提供统计信息：
- 总物品数量
- 各位置物品数量分布
- 识别到的位置标记列表
- 无法识别的异常数据

## 开始提取

请将微信聊天记录完整复制粘贴到下方，我将为你提取并生成 CSV 和 JSON 格式的物品清单。

---

**粘贴聊天记录区域：**

[在此粘贴完整的微信聊天记录]
